.idea/*
assets/*
test_resources/*
tests/*
.env

# macOS system files
.DS_Store

# Python bytecode (src/ and tools/ are run directly during boot and testing)
__pycache__/
*.pyc

# Ignore zip files in workflows folder
workflows/**/*.zip

# AI IDE configuration directories
.claude/*
.cursor/*
AGENTS.md
CLAUDE.md
CHANGELOG.md
.history/
.claude/hooks/

# Working material, not a deliverable: the ledger, specs, audits, review output.
# Local-only and never published — it carries absolute home paths and internal
# notes. git is NOT a backup for any of it; anything that must survive belongs
# in ~/brain.
docs/

/internal_docs/
